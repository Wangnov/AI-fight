# Background References

These are the original AI Fight background anchors used to regenerate stage art.
They intentionally stay separate from the generated shipping backgrounds so the
background pipeline does not accidentally reuse a previous generated result with
extra logo overlays or composition artifacts.

- `base_bg_arena.png`: original battle arena reference
- `base_menu_bg.png`: original opening/menu background reference
